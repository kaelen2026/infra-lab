{
  "name": "@infra/bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "飞书 IM 接待 bot：长连接收消息 → react → 安抚 notice → 派发到 infra-lab-bot workflow。",
  "scripts": {
    "build": "tsup",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "whoami": "tsx scripts/whoami.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "3.0.30",
    "@infra/env": "workspace:*",
    "@larksuiteoapi/node-sdk": "1.73.0",
    "ai": "7.0.65",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "tsx": "4.23.12"
  }
}
