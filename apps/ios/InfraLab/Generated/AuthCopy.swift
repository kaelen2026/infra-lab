// GENERATED by @infra/design — DO NOT EDIT. Run `pnpm gen:design`.
import Foundation

/// Canonical auth copy shared with web / android / harmony.
enum AuthCopy {
    static let brand = "infra-lab"
    static let tagline = "手机号，一步登录"

    enum Phone {
        static let title = "手机号登录"
        static let description = "未注册的手机号将自动创建账号。"
        static let label = "手机号"
        static let placeholder = "+8613800138000"
        static let submit = "获取验证码"
        static let submitBusy = "发送中…"
    }

    enum Code {
        static let title = "输入验证码"
        static let label = "6 位验证码"
        static let placeholder = "••••••"
        static let submit = "登录 / 注册"
        static let submitBusy = "验证中…"
        static let resend = "重新发送"
        static let changePhone = "‹ 更换手机号"

        static func description(phone: String, minutes: Int) -> String {
            "验证码已发送至 {phone}，{minutes} 分钟内有效。"
                .replacingOccurrences(of: "{phone}", with: phone)
                .replacingOccurrences(of: "{minutes}", with: String(minutes))
        }
        static func resendCooldown(seconds: Int) -> String {
            "{seconds}s 后重新发送".replacingOccurrences(of: "{seconds}", with: String(seconds))
        }
    }

    enum Done {
        static let title = "已登录"
        static let newAccount = "欢迎加入，新账号已自动创建。"
        static let logout = "退出登录"
    }

    static let footer = "原生客户端（iOS / Android / HarmonyOS）将改用 Bearer accessToken + refreshToken。"

    enum Errors {
        static let network = "网络异常，请稍后再试。"
        static let generic = "出错了，请稍后再试。"

        static func message(for code: AuthErrorCode) -> String {
            switch code {
        case .invalidRequest: return "手机号或验证码格式不正确。"
        case .resendCooldown: return "发送过于频繁，请稍后再试。"
        case .dailyLimitExceeded: return "今日验证码发送次数已达上限。"
        case .ipLimitExceeded: return "当前网络发送过于频繁，请稍后再试。"
        case .locked: return "尝试次数过多，账号已被临时锁定，请 10 分钟后重试。"
        case .codeExpired: return "验证码已过期，请重新获取。"
        case .invalidCode: return "验证码错误。"
        case .unauthorized: return "登录状态已失效，请重新登录。"
        case .invalidRefreshToken: return "登录状态已失效，请重新登录。"
        case .qrNotFound: return "二维码无效或已过期，请刷新后重试。"
        case .qrAlreadyUsed: return "该二维码已被使用，请刷新后重试。"
        case .qrNotApproved: return "尚未在手机上确认登录。"
            case .unknown: return generic
            }
        }
        static func invalidCodeRemaining(base: String, remaining: Int) -> String {
            "{base}还可尝试 {remaining} 次。"
                .replacingOccurrences(of: "{base}", with: base)
                .replacingOccurrences(of: "{remaining}", with: String(remaining))
        }
    }
}
