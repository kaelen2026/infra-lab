<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override: the dev API runs over plain HTTP on the host (10.0.2.2:3001),
  so cleartext is allowed for debug builds only. Release builds stay HTTPS-only.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:usesCleartextTraffic="true" />

</manifest>
