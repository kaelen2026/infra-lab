<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Night variants of the launch themes. The colors themselves already switch via
      values-night/colors.generated.xml (@color/window_background), so these only exist
      to make the day/night pairing explicit; keep them in sync with values/themes.xml.
    -->
    <style name="Theme.Infra" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="Theme.Infra.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/window_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Infra</item>
    </style>
</resources>
