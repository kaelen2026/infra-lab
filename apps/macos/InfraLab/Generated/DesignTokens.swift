// GENERATED by @infra/design — DO NOT EDIT. Run `pnpm gen:design`.
import SwiftUI
import AppKit

/// Canonical color + shape tokens shared with web / ios / android / harmony.
enum DesignTokens {
    static let radius: CGFloat = 10
    static let primary = dyn(light: "#B95115", dark: "#DF8352")
    static let primaryDeep = dyn(light: "#952400", dark: "#B95115")
    static let primaryForeground = dyn(light: "#FDFCF8", dark: "#1E130E")
    static let background = dyn(light: "#FCFAF4", dark: "#13110F")
    static let surface = dyn(light: "#FFFEFB", dark: "#1C1916")
    static let textPrimary = dyn(light: "#29231E", dark: "#EAE7E3")
    static let textSecondary = dyn(light: "#6E6762", dark: "#A5A19B")
    static let danger = dyn(light: "#C53732", dark: "#C74B47")
    static let border = dyn(light: "#E0DED8", dark: "#302D2A")

    /// A Color that resolves light/dark per the active NSAppearance.
    private static func dyn(light: String, dark: String) -> Color {
        Color(nsColor: NSColor(name: nil) { appearance in
            appearance.bestMatch(from: [.aqua, .darkAqua]) == .darkAqua
                ? NSColor(hex: dark)
                : NSColor(hex: light)
        })
    }
}

private extension NSColor {
    convenience init(hex: String) {
        let s = hex.hasPrefix("#") ? String(hex.dropFirst()) : hex
        var v: UInt64 = 0
        Scanner(string: s).scanHexInt64(&v)
        self.init(
            srgbRed: CGFloat((v >> 16) & 0xFF) / 255,
            green: CGFloat((v >> 8) & 0xFF) / 255,
            blue: CGFloat(v & 0xFF) / 255,
            alpha: 1
        )
    }
}
