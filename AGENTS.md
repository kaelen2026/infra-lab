# AGENTS.md

Guidance for Codex agents in this repo.

`CLAUDE.md` is the single source of truth for repository instructions. Read and
follow [`CLAUDE.md`](CLAUDE.md), including its referenced `.claude/docs/*` and
`.claude/rules/*` files.

Do not duplicate those instructions here; keeping this file as a pointer avoids
drift between agent guidance files.
